name: Continuous integration with lint check and pytest

on:
  pull_request:
    branches: [ main ]

jobs:
  lint:
    name: Run Ruff linting
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run Ruff lint
        uses: chartboost/ruff-action@v1
        with:
          version: "0.9.9"
          args: check .

      - name: Run Ruff format
        uses: chartboost/ruff-action@v1
        with:
          version: "0.9.9"
          args: format --check .

  poetry-lock-check:
    name: Check if poetry.lock is up to date
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: Set up poetry
        uses: abatilo/actions-poetry@v2
        with:
          poetry-version: "2.1.1"

      - name: Check if poetry.lock is in sync with pyproject.toml
        run: poetry check --lock

  test:
    name: Run tests
    runs-on: ubuntu-latest
    env:
      DATA_FOLDER_ID: ${{ secrets.DATA_FOLDER_ID }}
      KAKAO_REST_API_KEY: ${{ secrets.KAKAO_REST_API_KEY }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: Set up poetry
        uses: abatilo/actions-poetry@v2
        with:
          poetry-version: "2.1.1"

      - uses: actions/cache@v3
        name: Define a cache for the virtual environment based on the dependencies lock file
        with:
          path: ./.venv
          key: venv-${{ hashFiles('poetry.lock') }}

      - name: Install the project dependencies
        run: poetry install --without llm

      - name: Clean up disk space before tests
        run: |
          echo "=== Disk space before cleanup ==="
          df -h
          # Remove unnecessary files and directories
          rm -rf mlruns/*/artifacts 2>/dev/null || true
          rm -rf mlruns/*/models 2>/dev/null || true
          rm -rf result/test/* 2>/dev/null || true
          rm -rf result/untest/* 2>/dev/null || true
          rm -rf .pytest_cache 2>/dev/null || true
          find . -type d -name __pycache__ -exec rm -rf {} + 2>/dev/null || true
          find . -type f -name "*.pyc" -delete 2>/dev/null || true
          find . -type f -name "*.log" -delete 2>/dev/null || true
          find . -type f -name "*.pt" -delete 2>/dev/null || true
          find . -type f -name "*.pkl" -delete 2>/dev/null || true
          # Clean up poetry cache if needed
          poetry cache clear pypi --all -n 2>/dev/null || true
          echo "=== Disk space after cleanup ==="
          df -h

      - name: Run pytest
        run: |
          # Set environment variable to disable MLflow tracking during tests
          export MLFLOW_TRACKING_URI=""
          # Run pytest with minimal output and no cache
          poetry run pytest --cache-clear -v --tb=short --maxfail=5
          # Clean up immediately after each test run
          rm -rf mlruns/*/artifacts 2>/dev/null || true
          rm -rf result/test/* 2>/dev/null || true

      - name: Clean up after tests
        run: |
          echo "=== Final disk space check ==="
          df -h
          # Aggressive cleanup
          rm -rf .pytest_cache 2>/dev/null || true
          rm -rf mlruns/*/artifacts 2>/dev/null || true
          rm -rf mlruns/*/models 2>/dev/null || true
          rm -rf result/test/* 2>/dev/null || true
          rm -rf result/untest/* 2>/dev/null || true
          find . -type d -name __pycache__ -exec rm -rf {} + 2>/dev/null || true
          find . -type f -name "*.pyc" -delete 2>/dev/null || true
          find . -type f -name "*.log" -delete 2>/dev/null || true
          find . -type f -name "*.pt" -delete 2>/dev/null || true
          find . -type f -name "*.pkl" -delete 2>/dev/null || true
          # Clean up any large files in tmp directories
          find /tmp -name "*.tmp" -size +10M -delete 2>/dev/null || true
          echo "=== Disk space after final cleanup ==="
          df -h